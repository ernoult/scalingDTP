defaults:
  - base_options
  - dataset: cifar10.yaml
  - model: dtp
  - network: simple_vgg
  - optional model_network: ${model}_${network}
  - trainer: default.yaml
  - callbacks: default.yaml
  - logger: null  # use logger=wandb to enable wandb logging.
  - mode: default.yaml
  - _self_
  - override hydra/sweeper: orion